function palindromo( s,n,k) {

    if(s>=n){
        for (let index = 0; index < s.length; index++) {
           // const element = s[index];
           console.log(s.length);
           console.log(s.charAt());

        }
    }
}

palindromo('1234',4,5)